```kt
 private var audioEncoder: AudioEncoder? = null
```

```kt
   val success = VoiceCodecSDK.initialize(applicationContext)
if (success) {
    audioEncoder = AudioEncoder()
    LogUtils.getInstance().error("VoiceCodecSDK initialize success")
} else {
    LogUtils.getInstance().error("VoiceCodecSDK initialize fail")
}
```

```kt
  val encoderResult = audioEncoder!!.encode(map["file"].toString())
if (encoderResult.success) {
    LogUtils.getInstance().info("编码成功")
    val codes = encoderResult.codes // LongArray，编码序列
    val codesS = encoderResult.codesS // Long，编码⼤⼩
    val duration = encoderResult.originalDuration // 原始⾳频时⻓（秒）
    val sampleRate = encoderResult.originalSampleRate // 原始采样率
    sendTo(
        map["myPhone"].toString(),
        map["otherPhone"].toString(),
        codesS.toString(),
        (duration * 1000).toInt().toString(),
        codes!!.toList(),
        map["type"].toString(),
        LEVEL.HIGHT,
        result
    )
} 
```

# 录音部分（flutter）

- 框架
- https://pub.dev/packages/record

```dart
import 'package:record/record.dart';

final record = AudioRecorder();

// Check and request permission if needed
if (
await record.hasPermission()) {
// Start recording to file
await record.start( const RecordConfig( encoder: AudioEncoder.wav, sampleRate: 16000, numChannels: 1), path: 'aFullPath/myFile.wav');
}

// Stop recording...
final path = await record.stop();
// ... or cancel it (and implicitly remove file/blob).
await record.cancel();

record.dispose(
); // As always, don't forget this one.
```